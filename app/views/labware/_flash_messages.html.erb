<div id="flashes" class="row">
  <% flash.each do |name, message| %>
    <% Array(message).each do |m| %>
      <div class="alert alert-<%= flash_style(name) %>"><%= m %></div>
    <% end %>
  <% end %>
</div>
