<%# TODO Dry thit up with child_tube_creation.html.erb %>
<%= form_tag new_limber_plate_child_path(presenter.uuid), method: 'get' do %>
  <%= hidden_field_tag :purpose_uuid, uuid %>
  <%= submit_tag "Add an empty #{name} plate",
                 name:  nil,
                 class: "create-plate-button"
  %>
<% end %>

