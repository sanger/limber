{
  "plate":{
    "barcode": "<%= "#{@presenter.labware.barcode.prefix}#{@presenter.labware.barcode.number}" %>",
    "humanBarcode": "<%= "#{@presenter.labware.barcode}" %>",
    "uuid": "<%= @presenter.uuid %>",
    "state": "<%= @presenter.state %>",
    "purpose": "<%= @presenter.purpose_name %>",
    "preCapPools": <%= sorted_pre_cap_pool_json(@presenter.labware) %>,
    "tags": <%= @presenter.tag_sequences.to_json.html_safe %>,
    "tagGroupsList": <%= @presenter.tag_groups_list.to_json.html_safe %>
  }
}
