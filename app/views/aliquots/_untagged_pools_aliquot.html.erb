<div
  id="aliquot_<%= id %>"
  class="aliquot
        <%= container.location %>
        <%= container.state %>
        <%= container.suboptimal? ? 'suboptimal' : '' %>
        colour-<%= presenter.pool_index(container.pool_id) %>"
  rel="<%= "details_#{id}" %>"
  data-pool="<%= container.respond_to?(:pool_id) ? container.pool_id : '' %>">
  <span class="pool-id"><%= presenter.get_pool_group_id(container) %></span>
  <% if container.respond_to?(:contains_control?) && container.contains_control? %>
    <div class="control-type">
      <%= container.control_info_formatted %>
    </div>
  <%end%>
</div>
