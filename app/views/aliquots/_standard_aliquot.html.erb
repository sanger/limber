<div
  id="aliquot_<%= id %>"
  class="aliquot
        <%= container.location %>
        <%= container.state %>
        <%= container.suboptimal? ? 'suboptimal' : '' %>
        colour-<%= presenter.pool_index(container.pool_id) %>"
  rel="<%= "details_#{id}" %>"
  data-pool="<%= container.respond_to?(:pool_id) ? container.pool_id : '' %>"><%= presenter.pool_index(container.pool_id) %></div>
