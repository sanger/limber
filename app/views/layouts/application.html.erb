<!DOCTYPE html>
<html lang='en'>
  <head>
    <title>Limber<% if content_for?(:title) %> - <%= yield(:title) %><% end %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <%= csrf_meta_tag %>

    <%= vite_stylesheet_tag 'application' %>
    <%= vite_stylesheet_tag 'print', media: 'print' %>

    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application', defer: true %>

    <% if Rails.application.config.disable_animations %>
      <%= vite_stylesheet_tag 'disable-animations' %>
      <%= vite_javascript_tag 'disable-animations', defer: true %>
    <% end %>

    <link rel="apple-touch-icon" sizes="180x180" href="/<%= apple_touch_icon %>" />
    <link rel="icon" sizes="32x32" href="/icon32.png" />
    <link rel="icon" sizes="16x16" href="/icon16.png" />
    <link rel="shortcut icon" href="/<%= favicon %>" type="image/x-icon" />
  </head>
  <body class='<%= environment %> <%= logged_in_class %>'>
    <div id='app'>
      <%= render 'header' %>
      <%= render partial: 'flash_messages' %>
      <%= yield %>
    </div>
    <%= render partial: 'loading_modal' %>
    <%= render partial: 'version_information' %>
  </body>
</html>
