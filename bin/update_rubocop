#!/usr/bin/env ruby
# frozen_string_literal: true

# Find rubocop gems
cops = `bundle list`.scan(/rubocop[\w-]*/).join(' ')
puts "Updating #{cops}"
system("bundle update #{cops}")
puts "Correcting and ignoring..."
system("bundle exec rubocop --auto-correct --disable-uncorrectable")
